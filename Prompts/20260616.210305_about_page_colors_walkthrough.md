# User Prompt
the about page color is black on black.  Change it to Black on white background

# Walkthrough
I have configured the About page background to be solid white. This prevents readability issues caused by dark theme styles applying black backgrounds underneath the dark text colors.

## Changes Made
### Layout Files
- Modified [activity_about.xml](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Travel_Route_Helper_APK_GITHUB/app/src/main/res/layout/activity_about.xml):
  - Changed the root `CoordinatorLayout` background attribute to `@android:color/white`.
  - Added `app:cardBackgroundColor="@android:color/white"` to the `MaterialCardView` component.

## Verification
- Layout elements successfully compile and reference the system white color resources.
