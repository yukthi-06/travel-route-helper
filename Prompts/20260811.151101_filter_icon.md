<USER_REQUEST>
The word 'Filtered' appended to header is not visible due to word wrap.
Instead change the filter icon or icon color itself.
The filter icon looks like a 'Find' icon.  Change that to something else more appropritae.
</USER_REQUEST>

Plan:
1. Create a new vector drawable for the standard filter icon (e.g., `ic_filter.xml`).
2. Create a new vector drawable for the active filter icon (e.g., `ic_filter_active.xml`) with a yellow color to indicate it's active.
3. Update `menu_route_details.xml` to use the new standard filter icon.
4. Update `RouteDetailsActivity.java` to dynamically change the filter icon based on whether there are active filters in `onPrepareOptionsMenu` instead of setting a subtitle.
