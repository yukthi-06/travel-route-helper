<USER_REQUEST>
the GPS warning should also appear in Route details screen, when GPS is off
</USER_REQUEST>

Plan:
1. Add `tv_no_gps` TextView to `activity_route_details.xml`, mirroring the one in `activity_main.xml`. Update constraints of `recyclerViewPoints` so it's placed below `tv_no_gps`.
2. Copy the GPS checking logic from `MainActivity.java` to `RouteDetailsActivity.java`.
3. Check GPS status in `onResume()` and show/hide `tv_no_gps` accordingly.
