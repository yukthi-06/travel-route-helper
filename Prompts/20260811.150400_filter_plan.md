In the route details screen, in the menu items, add a new item 'Filter...'
On click of that, there should be a filter popup with checkboxes for 'Food','Toll','Petrol', 'Toilet'.
OOn checking one more more of the items and clicking 'Apply' button, the points list must be filtered by those items selcted in filter box.
When in filter mode, there should be some indication visually to show that we are in filtered mode

Plan:
1. Update `menu_route_details.xml` to include `action_filter`.
2. Update `RouteDetailsActivity.java` to handle `action_filter`.
3. Create a filter dialog in `RouteDetailsActivity` using `AlertDialog.Builder(this).setMultiChoiceItems(...)`.
4. Store selected filter types in a Set<String>.
5. Update `loadRouteData()` to apply the filter if the Set is not empty.
6. If the filter is applied, change the `Toolbar` subtitle to `(Filtered)`.
