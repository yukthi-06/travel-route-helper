# User Prompt
the about page color is black on black.  Change it to Black on white background

# Goal
Ensure the About page has a white background with dark text (black on white) by explicitly setting background colors on the root layout and the MaterialCardView.

## Proposed Changes

### Layout Files

#### [MODIFY] [activity_about.xml](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/Travel_Route_Helper_APK_GITHUB/app/src/main/res/layout/activity_about.xml)
- Change root `CoordinatorLayout` background from `@color/background` to `@android:color/white`.
- Add `app:cardBackgroundColor="@android:color/white"` to the `MaterialCardView` to keep the card white as well.

## Verification Plan

### Manual Verification
- Verify that the layout compiles.
- Open the About activity on the device. Confirm that the background is solid white and all text (app name, version, credits, build info) is clearly visible and black/dark.
