# User Prompt
the about page color is black on black.  Change it to Black on white background

# Tasks
- [x] Modify `activity_about.xml` to set background to `@android:color/white` on the root layout
- [x] Add `app:cardBackgroundColor="@android:color/white"` to `MaterialCardView` in `activity_about.xml`
- [x] Check text color properties in `activity_about.xml` to ensure they use dark colors (e.g. `@color/text_primary` / `@android:color/black`)
