<USER_REQUEST>
when there is no GPS, a bar appears with text 'GPS/Location not enabled'.  This covers the first route in the list.
When the warning bar appears, bring the list a bit down so that it is not covered
</USER_REQUEST>

Plan:
1. Update `activity_main.xml`.
2. Change the top constraint of the `recyclerViewRoutes` to be constrained to the bottom of the `tv_no_gps` warning text view, rather than the toolbar. This ensures that when the warning view is visible, it pushes the list down appropriately.
